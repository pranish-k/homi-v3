# Dependencies
node_modules/

# Builds
dist/
*.tsbuildinfo

# Env and secrets
.env
.env.*
!.env.example

# Logs and OS noise
*.log
.DS_Store

# Test artifacts
coverage/

# Local data volumes
.data/

# Expo artifacts if a command is run from the repo root instead of
# apps/mobile: it treats the root as the project and drops these here.
# Root-anchored so the real apps/*/tsconfig.json stay tracked.
/.expo/
/tsconfig.json
